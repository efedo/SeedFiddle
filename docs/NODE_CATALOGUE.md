# Current node catalogue

Generated by `python scripts/generate_node_catalogue.py`; do not edit by hand.
Optimization is image-local adaptation, not independent validation. Physical facts and scoring policies remain fixed.

## Project (`project`)

Current project, selected image, and image-local annotations

Optimization: Project data and references are not search parameters..

| Control | Default | Search status |
|---|---|---|

## Species and metadata (`metadata`)

Species, lot and notes

Optimization: Species and specimen metadata are facts..

| Control | Default | Search status |
|---|---|---|

## Species reference library (`species_reference_library`)

Resolve the exact immutable species-library pin and exclude this source

Optimization: Library provenance is fixed; consuming nodes own blend controls..

| Control | Default | Search status |
|---|---|---|

## Ruler detection and scale (`ruler_detection`)

Identify ruler geometry, tick families, units, and absolute scale

Optimization: Ruler length and tick spacing are physical facts..

| Control | Default | Search status |
|---|---|---|
| Expected visible span (`ruler_length_mm`) | 150.0 | Ruler length and tick spacing are physical facts. |
| Minor tick interval (`minor_tick_mm`) | 1.0 | Ruler length and tick spacing are physical facts. |

## Deskew and colour balance (`deskew_colour`)

Rectify from the colour card and apply gamut-safe neutral gains

Optimization: material.

| Control | Default | Search status |
|---|---|---|
| Apply neutral balance (`apply_colour_balance`) | True | Eligible when reviewed targets are available |
| Maximum correction (`max_deskew_degrees`) | 10.0 | Frozen annotation coordinate frame; independent calibration geometry is not supplied by seed masks. |
| Correct camera tilt (`apply_perspective_correction`) | True | Frozen annotation coordinate frame; independent calibration geometry is not supplied by seed masks. |
| Maximum perspective strength (`max_perspective_fraction`) | 0.25 | Frozen annotation coordinate frame; independent calibration geometry is not supplied by seed masks. |

## Layout detection (`layout_detection`)

Detect the Petri-dish edges and sample its exterior background annulus

Optimization: material.

| Control | Default | Search status |
|---|---|---|
| Detection resolution (`downsample_max_dimension`) | 1600 | Frozen calibration working resolution. |
| Rim support strictness (`hough_accumulator_threshold`) | 32 | Eligible when reviewed targets are available |
| Minimum radius / height (`min_radius_fraction`) | 0.16 | Eligible when reviewed targets are available |
| Maximum radius / height (`max_radius_fraction`) | 0.29 | Eligible when reviewed targets are available |
| Expected centre X (`expected_center_x_fraction`) | 0.58 | Eligible when reviewed targets are available |
| Expected centre Y (`expected_center_y_fraction`) | 0.4 | Eligible when reviewed targets are available |
| Fallback radius / height (`expected_radius_fraction`) | 0.225 | Eligible when reviewed targets are available |
| Expected outer diameter (`expected_outer_diameter_mm`) | 96.0 | Known physical vessel diameter. |
| Outer diameter tolerance (`calibrated_outer_radius_tolerance_fraction`) | 0.025 | Eligible when reviewed targets are available |
| Dual-rim search / radius (`rim_pair_search_fraction`) | 0.14 | Eligible when reviewed targets are available |
| Minimum rim separation (`rim_pair_min_separation_fraction`) | 0.025 | Eligible when reviewed targets are available |
| Maximum rim separation (`rim_pair_max_separation_fraction`) | 0.12 | Eligible when reviewed targets are available |
| Expected rim separation (`rim_pair_expected_separation_fraction`) | 0.045 | Eligible when reviewed targets are available |
| Secondary-rim support (`rim_pair_secondary_support_fraction`) | 0.3 | Eligible when reviewed targets are available |
| Outer-rim buffer (cm) (`perimeter_background_buffer_cm`) | 0.35 | Eligible when reviewed targets are available |
| Reference band thickness (cm) (`perimeter_background_band_thickness_cm`) | 0.5 | Eligible when reviewed targets are available |

## Hue only (`hue_only`)

Display corrected-image hue at fixed neutral darkness

Optimization: Fixed colour conversion has no adjustable calculation..

| Control | Default | Search status |
|---|---|---|

## Wavelet decomposition (`wavelet_decomposition`)

Undecimated multiscale B3-spline decomposition with exact reconstruction

Optimization: edges.

| Control | Default | Search status |
|---|---|---|
| Wavelet detail levels (`wavelet_level_count`) | 4 | Eligible when reviewed targets are available |

## Reference seed dimensions and shape (`seed_scale_estimation`)

Measure reviewed silhouettes and resolve joint dimensions/shape priors

Optimization: instances.

| Control | Default | Search status |
|---|---|---|
| Shape reference source (`shape_reference_source`) | Current image only | Frozen training-source policy and provenance. |
| Reference correction (`reference_scale_factor`) | 0.72 | Eligible when reviewed targets are available |
| Reference ROI left (`reference_roi_x_min`) | 0.43 | Eligible when reviewed targets are available |
| Reference ROI right (`reference_roi_x_max`) | 0.6 | Eligible when reviewed targets are available |
| Reference ROI top (`reference_roi_y_min`) | 0.66 | Eligible when reviewed targets are available |
| Reference ROI bottom (`reference_roi_y_max`) | 0.84 | Eligible when reviewed targets are available |
| Reference colour distance (`reference_colour_distance_threshold`) | 10.0 | Eligible when reviewed targets are available |
| Maximum component aspect (`reference_max_aspect_ratio`) | 3.0 | Eligible when reviewed targets are available |
| Components used (`reference_max_components`) | 3 | Eligible when reviewed targets are available |
| Fallback diameter / dish radius (`fallback_diameter_fraction`) | 0.16 | Eligible when reviewed targets are available |
| Boundary sensitivity radius (`shape_boundary_perturbation_radius_px`) | 2 | Fixed reference sensitivity convention. |
| Scale uncertainty (`shape_calibration_uncertainty_fraction`) | 0.01 | Measured calibration uncertainty. |
| Contour samples (`shape_contour_samples`) | 128 | Eligible when reviewed targets are available |
| Minimum physical seeds / component (`shape_minimum_component_seeds`) | 2 | Eligible when reviewed targets are available |
| Hierarchy shrinkage support (`shape_shrinkage_seed_count`) | 5.0 | Eligible when reviewed targets are available |
| Maximum contour modes (`shape_maximum_contour_modes`) | 4 | Eligible when reviewed targets are available |
| Use shape prior for oval candidates (`shape_use_prior_for_oval_candidates`) | False | Eligible when reviewed targets are available |
| Use shape prior for procedural candidates (`shape_use_prior_for_procedural`) | False | Eligible when reviewed targets are available |

## Material colour probabilities (`background_likelihood`)

Independent foreground, background, and Other colour evidence

Optimization: colour.

| Control | Default | Search status |
|---|---|---|
| Use background colour analysis (`background_colour_enabled`) | True | Eligible when reviewed targets are available |
| Reference brush radius / diameter (`background_sample_radius_fraction`) | 0.1 | Eligible when reviewed targets are available |
| Automatic chroma percentile (`background_chroma_percentile`) | 50.0 | Eligible when reviewed targets are available |
| Automatic lightness percentile (`background_lightness_percentile`) | 55.0 | Eligible when reviewed targets are available |
| Minimum automatic area (`background_minimum_sample_fraction`) | 0.002 | Eligible when reviewed targets are available |
| Perimeter colour tolerance (`background_prior_tolerance`) | 24.0 | Eligible when reviewed targets are available |
| Keep perimeter reference (`background_keep_perimeter_reference`) | True | Eligible when reviewed targets are available |
| Lightness range floor (`background_lightness_scale_floor`) | 8.0 | Eligible when reviewed targets are available |
| Chroma range floor (`background_chroma_scale_floor`) | 3.0 | Eligible when reviewed targets are available |
| Maximum reference colour modes (`background_colour_components`) | 32 | Eligible when reviewed targets are available |
| Distribution fit rounds (`background_distribution_fit_iterations`) | 6 | Eligible when reviewed targets are available |
| Mode frequency influence (`background_frequency_weight_power`) | 0.35 | Eligible when reviewed targets are available |
| Colour tolerance multiplier (`background_distribution_scale_multiplier`) | 1.25 | Eligible when reviewed targets are available |
| Perimeter evidence floor (`background_automatic_evidence_floor`) | 0.1 | Eligible when reviewed targets are available |
| Perimeter authority half-life / seed areas (`background_reviewed_authority_half_life_seed_areas`) | 0.5 | Eligible when reviewed targets are available |
| Foreground reference source (`foreground_reference_source`) | Species library + current image | Frozen training-source policy and provenance. |
| Current-image reference weight (`foreground_current_reference_weight`) | 1.0 | Eligible when reviewed targets are available |
| Outer-rim area used (`inner_radius_fraction`) | 0.9 | Eligible when reviewed targets are available |
| Lab chroma weight (`foreground_chroma_weight`) | 1.8 | Eligible when reviewed targets are available |
| Foreground colour influence (`foreground_reference_weight`) | 0.75 | Eligible when reviewed targets are available |
| Include annotated seeds as Foreground (`foreground_include_annotated_seed_instances`) | True | Eligible when reviewed targets are available |
| Maximum reference colour modes (`foreground_reference_components`) | 64 | Eligible when reviewed targets are available |
| Mode frequency influence (`foreground_frequency_weight_power`) | 0.0 | Eligible when reviewed targets are available |
| Colour tolerance multiplier (`foreground_distribution_scale_multiplier`) | 1.5 | Eligible when reviewed targets are available |

## Material noise probabilities (`refined_background_likelihood`)

Independent foreground, background, and Other directional texture evidence

Optimization: noise.

| Control | Default | Search status |
|---|---|---|
| Use background noise analysis (`background_noise_enabled`) | True | Eligible when reviewed targets are available |
| Foreground texture reference source (`foreground_noise_reference_source`) | Species library + current image | Frozen training-source policy and provenance. |
| Current-image reference weight (`foreground_noise_current_reference_weight`) | 1.0 | Eligible when reviewed targets are available |
| Use foreground noise analysis (`foreground_noise_enabled`) | True | Eligible when reviewed targets are available |
| Medium band / diameter (`noise_medium_scale_fraction`) | 0.03 | Eligible when reviewed targets are available |
| Coarse band / diameter (`noise_coarse_scale_fraction`) | 0.08 | Eligible when reviewed targets are available |
| Direction interval (degrees) (`noise_direction_step_degrees`) | 15 | Eligible when reviewed targets are available |
| Ray length / diameter (`noise_vector_length_fraction`) | 0.55 | Eligible when reviewed targets are available |
| Samples per ray (`noise_vector_sample_count`) | 9 | Eligible when reviewed targets are available |
| Ray sample decay (`noise_vector_decay`) | 0.86 | Eligible when reviewed targets are available |
| Direction integration (`noise_direction_integration`) | maximum | Eligible when reviewed targets are available |
| GPU working dimension (`noise_working_maximum_dimension`) | 1280 | Frozen analysis resolution and resource policy. |
| Medium band / diameter (`foreground_noise_medium_scale_fraction`) | 0.03 | Eligible when reviewed targets are available |
| Coarse band / diameter (`foreground_noise_coarse_scale_fraction`) | 0.08 | Eligible when reviewed targets are available |
| Direction interval (degrees) (`foreground_noise_direction_step_degrees`) | 15 | Eligible when reviewed targets are available |
| Ray length / diameter (`foreground_noise_vector_length_fraction`) | 0.55 | Eligible when reviewed targets are available |
| Samples per ray (`foreground_noise_vector_sample_count`) | 9 | Eligible when reviewed targets are available |
| Ray sample decay (`foreground_noise_vector_decay`) | 0.86 | Eligible when reviewed targets are available |
| Direction integration (`foreground_noise_direction_integration`) | 1st tertile | Eligible when reviewed targets are available |
| GPU working dimension (`foreground_noise_working_maximum_dimension`) | 1280 | Frozen analysis resolution and resource policy. |

## Edge gradients (`edge_gradients`)

Configurable original/wavelet gradients plus both tangent encodings

Optimization: edges.

| Control | Default | Search status |
|---|---|---|
| Gradient method (`edge_gradient_method`) | scharr | Eligible when reviewed targets are available |
| Source fusion (`edge_gradient_source_fusion`) | maximum | Eligible when reviewed targets are available |
| Include original image (`edge_gradient_include_original`) | True | Eligible when reviewed targets are available |
| Use despeckled grayscale for original (`edge_gradient_use_despeckled_flattened`) | False | Eligible when reviewed targets are available |
| Include wavelet detail 1 (`edge_gradient_include_wavelet_detail_1`) | False | Eligible when reviewed targets are available |
| Include wavelet detail 2 (`edge_gradient_include_wavelet_detail_2`) | False | Eligible when reviewed targets are available |
| Include wavelet detail 3 (`edge_gradient_include_wavelet_detail_3`) | False | Eligible when reviewed targets are available |
| Include wavelet detail 4 (`edge_gradient_include_wavelet_detail_4`) | False | Eligible when reviewed targets are available |
| Include wavelet residual (`edge_gradient_include_wavelet_residual`) | False | Eligible when reviewed targets are available |
| Wavelet detail gain (`edge_wavelet_detail_gain`) | 1.0 | Eligible when reviewed targets are available |
| Pre-edge blur sigma (`edge_blur_sigma`) | 1.2 | Eligible when reviewed targets are available |
| Edge chroma weight (`edge_chroma_weight`) | 1.5 | Eligible when reviewed targets are available |
| Strength normalization percentile (`edge_normalization_percentile`) | 99.0 | Eligible when reviewed targets are available |
| Edge-strength gamma (`edge_strength_gamma`) | 0.65 | Eligible when reviewed targets are available |
| Normal sampling step (`ridge_nms_step_px`) | 1.0 | Eligible when reviewed targets are available |
| Hysteresis low (`ridge_low_threshold`) | 0.1 | Eligible when reviewed targets are available |
| Hysteresis high (`ridge_high_threshold`) | 0.24 | Eligible when reviewed targets are available |
| Hysteresis reach (`ridge_hysteresis_iterations`) | 8 | Eligible when reviewed targets are available |

## Directional surface darkness gradients (`surface_darkness_gradients`)

Find maximum one-sided lightening and darkening L* slopes

Optimization: instances.

| Control | Default | Search status |
|---|---|---|
| Surface blur sigma (`surface_gradient_blur_sigma`) | 1.6 | Eligible when reviewed targets are available |
| Ray length / diameter (`surface_gradient_radius_fraction`) | 0.45 | Eligible when reviewed targets are available |
| Direction step (`surface_gradient_direction_step_degrees`) | 15 | Eligible when reviewed targets are available |
| Samples per ray (`surface_gradient_sample_count`) | 8 | Eligible when reviewed targets are available |
| Strength normalization percentile (`surface_gradient_normalization_percentile`) | 99.0 | Eligible when reviewed targets are available |
| Surface-strength gamma (`surface_gradient_strength_gamma`) | 0.6 | Eligible when reviewed targets are available |
| GPU working dimension (`surface_gradient_working_maximum_dimension`) | 1280 | Frozen analysis resolution and resource policy. |

## Multiscale darkness and colour noise (`frequency_noise_masks`)

Measure surrounding darkness and Lab-colour energy in three bands

Optimization: texture.

| Control | Default | Search status |
|---|---|---|
| Fine scale / diameter (`frequency_noise_fine_scale_fraction`) | 0.008 | Eligible when reviewed targets are available |
| Medium scale / diameter (`frequency_noise_medium_scale_fraction`) | 0.03 | Eligible when reviewed targets are available |
| Coarse scale / diameter (`frequency_noise_coarse_scale_fraction`) | 0.1 | Eligible when reviewed targets are available |
| RMS context / diameter (`frequency_noise_context_fraction`) | 0.025 | Eligible when reviewed targets are available |
| Mask normalization percentile (`frequency_noise_normalization_percentile`) | 99.0 | Eligible when reviewed targets are available |
| Noise-energy gamma (`frequency_noise_strength_gamma`) | 0.65 | Eligible when reviewed targets are available |
| GPU working dimension (`frequency_noise_working_maximum_dimension`) | 1280 | Frozen analysis resolution and resource policy. |

## Reference texture prototypes (`reference_texture_prototypes`)

Learn material prototypes and instance-derived edge classes

Optimization: texture.

| Control | Default | Search status |
|---|---|---|
| Foreground prototype source (`material_prototype_reference_source`) | Species library + current image | Frozen training-source policy and provenance. |
| Current-image reference weight (`material_prototype_current_reference_weight`) | 1.0 | Eligible when reviewed targets are available |
| Maximum material prototypes / class (`reference_texture_material_prototypes_per_class`) | 64 | Eligible when reviewed targets are available |
| Minimum support / prototype (`reference_texture_minimum_samples_per_prototype`) | 16 | Eligible when reviewed targets are available |
| Prototype fit iterations (`reference_texture_fit_iterations`) | 4 | Eligible when reviewed targets are available |
| Prototype tolerance (`reference_texture_similarity_scale`) | 1.0 | Eligible when reviewed targets are available |
| Material class contrast (`reference_texture_class_contrast`) | 4.0 | Eligible when reviewed targets are available |
| Feature context / diameter (`reference_texture_context_fraction`) | 0.04 | Eligible when reviewed targets are available |
| Collage patch / diameter (`reference_texture_patch_fraction`) | 0.28 | Eligible when reviewed targets are available |
| GPU working dimension (`reference_texture_working_maximum_dimension`) | 960 | Frozen analysis resolution and resource policy. |

## Material evidence decision (`material_evidence_decision`)

Calibrate Seed versus Non-seed with explicit ambiguity and unknown mass

Optimization: material.

| Control | Default | Search status |
|---|---|---|
| Colour evidence weight (`material_colour_weight`) | 1.0 | Eligible when reviewed targets are available |
| Texture evidence weight (`material_noise_weight`) | 0.55 | Eligible when reviewed targets are available |
| Prototype evidence weight (`material_prototype_weight`) | 0.7 | Eligible when reviewed targets are available |
| Unknown evidence weight (`material_unknown_weight`) | 0.35 | Eligible when reviewed targets are available |
| Decision temperature (`material_decision_temperature`) | 1.0 | Eligible when reviewed targets are available |
| Seed proposal threshold (`material_seed_threshold`) | 0.68 | Eligible when reviewed targets are available |
| Proposal morphology / diameter (`material_morphology_fraction`) | 0.06 | Eligible when reviewed targets are available |

## Reference seed traits (`reference_seed_traits`)

Classify reviewed seed coat patterns and non-exclusive conditions

Optimization: traits.

| Control | Default | Search status |
|---|---|---|
| Trait reference source (`seed_trait_reference_source`) | Species library + current image | Frozen training-source policy and provenance. |
| Current-image reference weight (`seed_trait_current_reference_weight`) | 1.0 | Eligible when reviewed targets are available |
| Maximum prototypes / class (`reference_seed_trait_prototypes_per_class`) | 64 | Eligible when reviewed targets are available |
| Minimum support / prototype (`reference_seed_trait_minimum_samples_per_prototype`) | 16 | Eligible when reviewed targets are available |
| Prototype fit iterations (`reference_seed_trait_fit_iterations`) | 4 | Eligible when reviewed targets are available |
| Prototype tolerance (`reference_seed_trait_similarity_scale`) | 1.0 | Eligible when reviewed targets are available |
| Class contrast (`reference_seed_trait_class_contrast`) | 4.0 | Eligible when reviewed targets are available |
| Feature context / diameter (`reference_seed_trait_context_fraction`) | 0.04 | Eligible when reviewed targets are available |
| Contour exclusion / diameter (`reference_seed_trait_interior_buffer_fraction`) | 0.08 | Eligible when reviewed targets are available |
| GPU working dimension (`reference_seed_trait_working_maximum_dimension`) | 1280 | Frozen analysis resolution and resource policy. |

## Reference edges (`reference_edge_probability`)

Classify edge context, require true-edge support, normalize, and thin

Optimization: edges.

| Control | Default | Search status |
|---|---|---|
| Edge prototype source (`edge_prototype_reference_source`) | Species library + current image | Frozen training-source policy and provenance. |
| Current-image reference weight (`edge_prototype_current_reference_weight`) | 1.0 | Eligible when reviewed targets are available |
| Non-physical subtraction weight (`net_physical_edge_internal_scale`) | 0.5 | Eligible when reviewed targets are available |
| Maximum edge prototypes / class (`reference_texture_edge_prototypes_per_class`) | 256 | Eligible when reviewed targets are available |
| Minimum edge support / prototype (`reference_edge_minimum_samples_per_prototype`) | 16 | Eligible when reviewed targets are available |
| Edge prototype fit iterations (`reference_edge_fit_iterations`) | 4 | Eligible when reviewed targets are available |
| Edge prototype tolerance (`reference_edge_similarity_scale`) | 1.0 | Eligible when reviewed targets are available |
| Edge class contrast (`reference_edge_class_contrast`) | 4.0 | Eligible when reviewed targets are available |
| Edge GPU working dimension (`reference_texture_edge_working_maximum_dimension`) | 2048 | Frozen analysis resolution and resource policy. |
| Minimum edge seed diameter / px (`reference_edge_minimum_working_seed_diameter_px`) | 28.0 | Frozen analysis resolution and resource policy. |
| Strip side offset / diameter (`reference_edge_strip_normal_offset_fraction`) | 0.05 | Eligible when reviewed targets are available |
| Strip tangent half-length / diameter (`reference_edge_strip_tangent_half_length_fraction`) | 0.08 | Eligible when reviewed targets are available |
| Internal-candidate ridge weight (`reference_edge_ridge_weight`) | 0.35 | Eligible when reviewed targets are available |
| Interior buffer / diameter (`reference_texture_instance_interior_buffer_fraction`) | 0.08 | Eligible when reviewed targets are available |
| Normal sampling step (`reference_ridge_nms_step_px`) | 1.0 | Eligible when reviewed targets are available |
| Hysteresis low (`reference_ridge_low_threshold`) | 0.1 | Eligible when reviewed targets are available |
| Hysteresis high (`reference_ridge_high_threshold`) | 0.24 | Eligible when reviewed targets are available |
| Hysteresis reach (`reference_ridge_hysteresis_iterations`) | 8 | Eligible when reviewed targets are available |
| GPU working dimension (`reference_ridge_working_maximum_dimension`) | 1280 | Frozen analysis resolution and resource policy. |
| Local normalization radius / diameter (`reference_edge_normalization_radius_fraction`) | 0.3 | Eligible when reviewed targets are available |
| Target local support (`reference_edge_normalization_target_support`) | 0.35 | Eligible when reviewed targets are available |
| Maximum local gain (`reference_edge_normalization_maximum_gain`) | 2.5 | Eligible when reviewed targets are available |
| Absolute support floor (`reference_edge_normalization_absolute_floor`) | 0.04 | Eligible when reviewed targets are available |

## Oriented edge traces (`edge_traces`)

Link tangent/curvature-compatible ridges, bridge gaps and split junctions

Optimization: instances.

| Control | Default | Search status |
|---|---|---|
| Ridge source (`trace_edge_source`) | generic_ridges | Eligible when reviewed targets are available |
| Trace diameter / master diameter (`trace_diameter_multiplier`) | 1.0 | Eligible when reviewed targets are available |
| Link tangent tolerance (`trace_tangent_tolerance_degrees`) | 24.0 | Eligible when reviewed targets are available |
| Maximum trace gap (`trace_maximum_gap_px`) | 2 | Eligible when reviewed targets are available |
| Convexity bias (`trace_curvature_policy`) | prefer | Eligible when reviewed targets are available |
| Curvature ambiguity (`trace_curvature_tolerance_degrees`) | 6.0 | Eligible when reviewed targets are available |
| Continuity window / diameter (`trace_window_fraction`) | 0.2 | Eligible when reviewed targets are available |
| Continuity samples (`trace_sample_count`) | 7 | Eligible when reviewed targets are available |
| Minimum trace length / diameter (`trace_minimum_length_fraction`) | 0.18 | Eligible when reviewed targets are available |
| Junction neighbour limit (`trace_junction_max_neighbors`) | 4 | Eligible when reviewed targets are available |

## Seed-boundary confirmation (`seed_edge_curves`)

Trace, radius, circle, ellipse, centre-vote and semantic confirmation

Optimization: instances.

| Control | Default | Search status |
|---|---|---|
| Master diameter multiplier (`curve_diameter_multiplier`) | 1.0 | Eligible when reviewed targets are available |
| Minimum fitted radius / diameter (`boundary_radius_min_fraction`) | 0.25 | Eligible when reviewed targets are available |
| Maximum fitted radius / diameter (`boundary_radius_max_fraction`) | 0.8 | Eligible when reviewed targets are available |
| Radius hypotheses (`boundary_radius_sample_count`) | 9 | Eligible when reviewed targets are available |
| Arc span (`boundary_arc_span_degrees`) | 70.0 | Eligible when reviewed targets are available |
| Arc samples (`boundary_arc_sample_count`) | 15 | Eligible when reviewed targets are available |
| Arc tangent tolerance (`boundary_orientation_tolerance_degrees`) | 20.0 | Eligible when reviewed targets are available |
| Missing-edge floor (`boundary_missing_support_floor`) | 0.15 | Eligible when reviewed targets are available |
| Circle residual tolerance (`boundary_circle_residual_tolerance`) | 0.16 | Eligible when reviewed targets are available |
| Ellipse residual tolerance (`boundary_ellipse_residual_tolerance`) | 0.18 | Eligible when reviewed targets are available |
| Maximum ellipse axis ratio (`boundary_max_axis_ratio`) | 2.2 | Eligible when reviewed targets are available |
| Radius-prior tolerance (`boundary_radius_log_tolerance`) | 0.45 | Eligible when reviewed targets are available |
| Centre-vote influence (`boundary_center_vote_weight`) | 0.35 | Eligible when reviewed targets are available |
| Centre-vote blur / diameter (`boundary_center_vote_blur_fraction`) | 0.08 | Eligible when reviewed targets are available |
| Semantic-side influence (`boundary_semantic_weight`) | 0.25 | Eligible when reviewed targets are available |
| Instance-edge influence (`boundary_instance_edge_influence`) | 0.35 | Eligible when reviewed targets are available |
| Non-physical discount (`boundary_nonphysical_edge_discount`) | 0.8 | Eligible when reviewed targets are available |
| Lightness-polarity boost (`boundary_polarity_boost`) | 0.2 | Eligible when reviewed targets are available |
| Accepted-boundary minimum (`boundary_minimum_confidence`) | 0.12 | Eligible when reviewed targets are available |
| Maximum oval candidates (`boundary_geometry_max_candidates`) | 256 | Eligible when reviewed targets are available |
| GPU working dimension (`boundary_working_maximum_dimension`) | 1280 | Frozen analysis resolution and resource policy. |

## Boundary confidence and normals (`boundary_normals`)

Estimate boundary strength and continuous outward normal direction

Optimization: boundary.

| Control | Default | Search status |
|---|---|---|
| Boundary width / diameter (`boundary_width_fraction`) | 0.025 | Eligible when reviewed targets are available |

## Grayscale and local lighting (`illumination_decomposition`)

Flatten grayscale and identify locally unusual shadow/highlight areas

Optimization: instances.

| Control | Default | Search status |
|---|---|---|
| Illumination scale / diameter (`illumination_scale_fraction`) | 0.55 | Eligible when reviewed targets are available |
| Flattened contrast gain (`flattening_contrast_gain`) | 1.8 | Eligible when reviewed targets are available |
| Maximum speckle diameter / seed diameter (`despeckle_maximum_diameter_fraction`) | 0.06 | Eligible when reviewed targets are available |
| Minimum darkness below surround (`despeckle_minimum_darkness_levels`) | 18.0 | Eligible when reviewed targets are available |
| Peripheral sampling width / diameter (`despeckle_periphery_width_fraction`) | 0.025 | Eligible when reviewed targets are available |
| Minimum lighter surround fraction (`despeckle_minimum_lighter_surround_fraction`) | 0.9 | Eligible when reviewed targets are available |
| Extreme context / diameter (`lighting_deviation_scale_fraction`) | 0.1 | Eligible when reviewed targets are available |
| Shadow deviation threshold (`shadow_z_threshold`) | 0.75 | Eligible when reviewed targets are available |
| Highlight deviation threshold (`highlight_z_threshold`) | 0.75 | Eligible when reviewed targets are available |
| Extreme transition softness (`lighting_extreme_softness`) | 0.3 | Eligible when reviewed targets are available |

## Image-quality diagnostics (`image_quality`)

Map focus, clipping, underexposure, glare, and local noise risk

Optimization: reviewed raster.

| Control | Default | Search status |
|---|---|---|
| Noise scale / diameter (`quality_noise_scale_fraction`) | 0.025 | Eligible when reviewed targets are available |

## Procedural seed separation (`procedural_instances`)

Fuse material, physical-boundary and seed-scale evidence into reviewable instances

Optimization: instances.

| Control | Default | Search status |
|---|---|---|
| Topology working dimension (`working_maximum_dimension`) | 1600 | Frozen analysis resolution and resource policy. |
| Material threshold scale (`foreground_threshold_scale`) | 0.82 | Eligible when reviewed targets are available |
| Material closing / diameter (`occupancy_closing_fraction`) | 0.12 | Eligible when reviewed targets are available |
| Filled-hole area / diameter² (`occupancy_hole_area_fraction`) | 1.25 | Eligible when reviewed targets are available |
| Reference-surface occupancy weight (`reference_texture_weight`) | 0.35 | Legacy fallback only; resolved material is authoritative. |
| Dish margin / diameter (`dish_margin_fraction`) | 0.12 | Eligible when reviewed targets are available |
| Candidate edge weight (`boundary_edge_weight`) | 0.35 | Eligible when reviewed targets are available |
| Candidate ridge weight (`boundary_ridge_weight`) | 0.65 | Eligible when reviewed targets are available |
| Unclassified-edge floor (`boundary_semantic_floor`) | 0.08 | Eligible when reviewed targets are available |
| Non-physical discount (`boundary_nonphysical_discount`) | 0.95 | Eligible when reviewed targets are available |
| Reference-edge ridge weight (`boundary_physical_ridge_weight`) | 0.55 | Eligible when reviewed targets are available |
| Oriented-trace weight (`boundary_trace_weight`) | 0.45 | Eligible when reviewed targets are available |
| Surface-darkening boundary weight (`boundary_surface_darkening_weight`) | 0.3 | Eligible when reviewed targets are available |
| Trace minimum / diameter (`trace_minimum_length_fraction`) | 0.22 | Eligible when reviewed targets are available |
| Trace convexity weight (`trace_convexity_weight`) | 0.6 | Eligible when reviewed targets are available |
| Centre smoothing / diameter (`centre_geometry_smoothing_fraction`) | 0.14 | Eligible when reviewed targets are available |
| Centre material weight (`centre_material_weight`) | 0.2 | Eligible when reviewed targets are available |
| Centre interior-depth weight (`centre_distance_weight`) | 0.2 | Eligible when reviewed targets are available |
| Blurred flattened-gray weight (`centre_flattened_grayscale_weight`) | 0.6 | Eligible when reviewed targets are available |
| Validated oval-centre weight (`centre_validated_oval_weight`) | 0.85 | Eligible when reviewed targets are available |
| Marker spacing / diameter (`centre_minimum_separation_fraction`) | 0.42 | Eligible when reviewed targets are available |
| Sparse marker spacing / diameter (`sparse_centre_minimum_separation_fraction`) | 0.58 | Eligible when reviewed targets are available |
| Expected sparse seed area / diameter² (`sparse_seed_area_fraction`) | 0.47 | Eligible when reviewed targets are available |
| Expected packed cell area / diameter² (`packed_seed_cell_fraction`) | 0.72 | Eligible when reviewed targets are available |
| Expected marker-count multiplier (`marker_count_multiplier`) | 1.02 | Eligible when reviewed targets are available |
| Minimum marker likelihood (`minimum_marker_score`) | 0.12 | Eligible when reviewed targets are available |
| Hard minimum area / diameter² (`minimum_instance_area_fraction`) | 0.26 | Eligible when reviewed targets are available |
| Soft minimum area / diameter² (`soft_minimum_instance_area_fraction`) | 0.39 | Eligible when reviewed targets are available |
| Hard maximum area / diameter² (`maximum_instance_area_fraction`) | 1.45 | Eligible when reviewed targets are available |
| Soft maximum width / diameter (`soft_maximum_instance_width_fraction`) | 1.15 | Eligible when reviewed targets are available |
| Hard maximum width / diameter (`hard_maximum_instance_width_fraction`) | 1.35 | Eligible when reviewed targets are available |
| Hard maximum concavity (`maximum_internal_concavity_fraction`) | 0.15 | Eligible when reviewed targets are available |
| Hard maximum thin protrusion (`maximum_protrusion_area_fraction`) | 0.1 | Eligible when reviewed targets are available |
| Hard minimum solidity (`minimum_instance_solidity`) | 0.62 | Eligible when reviewed targets are available |
| Hard maximum axis ratio (`maximum_instance_axis_ratio`) | 2.8 | Eligible when reviewed targets are available |
| Candidates per centre (`candidate_hypotheses_per_marker`) | 5 | Eligible when reviewed targets are available |
| Candidate overlap tolerance (`candidate_overlap_fraction`) | 0.02 | Eligible when reviewed targets are available |
| Overreach cost weight (`reference_error_overreach_weight`) | 2.0 | Fixed scoring policy or coverage declaration; never minimize the penalty itself. |
| Overreach distance scale / diameter (`reference_error_distance_scale_fraction`) | 0.5 | Fixed scoring policy or coverage declaration; never minimize the penalty itself. |
| Minimum matching overlap (IoU) (`reference_error_minimum_match_iou`) | 0.2 | Fixed scoring policy or coverage declaration; never minimize the penalty itself. |
| Missed seed pixel cost (`reference_error_missed_seed_weight`) | 0.5 | Fixed scoring policy or coverage declaration; never minimize the penalty itself. |
| Incorrect concavity extra cost (`reference_error_concavity_weight`) | 2.0 | Fixed scoring policy or coverage declaration; never minimize the penalty itself. |
| Annotations cover whole dish (`reference_error_annotations_complete`) | False | Fixed scoring policy or coverage declaration; never minimize the penalty itself. |

## U-Net + watershed instances (`unet_instances`)

Distinguish physical contacts from non-physical coat-pattern boundaries

Optimization: unet.

| Control | Default | Search status |
|---|---|---|
| Checkpoint path (`checkpoint_path`) | models/unet_seed_instances.pt | Checkpoint identity; use Learning to train or select a model. |
| Inference tile size (`tile_size`) | 512 | Frozen inference tiling policy. |
| Tile overlap (`tile_overlap`) | 96 | Frozen inference tiling policy. |
| Interior threshold (`interior_threshold`) | 0.5 | Eligible when reviewed targets are available |
| Centre threshold (`centre_threshold`) | 0.3 | Eligible when reviewed targets are available |
| Centre spacing / diameter (`centre_minimum_separation_fraction`) | 0.32 | Eligible when reviewed targets are available |
| Minimum area / diameter squared (`minimum_instance_area_fraction`) | 0.15 | Eligible when reviewed targets are available |
| Physical-boundary weight (`physical_boundary_weight`) | 0.72 | Eligible when reviewed targets are available |
| Distance-topography weight (`distance_topography_weight`) | 0.28 | Eligible when reviewed targets are available |
| Pattern-boundary discount (`pattern_boundary_discount`) | 0.8 | Eligible when reviewed targets are available |
| Uncertainty penalty (`uncertainty_penalty`) | 0.2 | Eligible when reviewed targets are available |
| Foreground erosion / diameter (`foreground_erosion_fraction`) | 0.0 | Eligible when reviewed targets are available |

## StarDist seed instances (`stardist_instances`)

Predict one scored star-convex polygon for each visible seed

Optimization: stardist.

| Control | Default | Search status |
|---|---|---|
| Checkpoint path (`checkpoint_path`) | models/stardist_seed_instances.pt | Checkpoint identity; use Learning to train or select a model. |
| Inference tile size (`tile_size`) | 512 | Frozen inference tiling policy. |
| Tile overlap (`tile_overlap`) | 96 | Frozen inference tiling policy. |
| Object threshold (`object_threshold`) | 0.45 | Eligible when reviewed targets are available |
| NMS overlap threshold (`nms_iou_threshold`) | 0.35 | Eligible when reviewed targets are available |
| Peak radius / diameter (`local_maximum_radius_fraction`) | 0.08 | Eligible when reviewed targets are available |
| Minimum area / diameter squared (`minimum_instance_area_fraction`) | 0.15 | Eligible when reviewed targets are available |
| Maximum polygon candidates (`maximum_candidates`) | 4096 | Eligible when reviewed targets are available |

## Wrinkling likelihood (`wrinkling`)

Detect seed-relative ridge and valley texture away from boundaries

Optimization: wrinkling.

| Control | Default | Search status |
|---|---|---|
| Wrinkle scale / diameter (`wrinkle_scale_fraction`) | 0.035 | Eligible when reviewed targets are available |

## Coat-pattern decomposition (`pattern_decomposition`)

Provide broad plain, spotted, mottled, patch, stripe, and bicolour probabilities

Optimization: reviewed raster.

| Control | Default | Search status |
|---|---|---|
| Pattern scale / diameter (`pattern_scale_fraction`) | 0.18 | Eligible when reviewed targets are available |

## Broad colour probabilities (`colour_probabilities`)

Estimate white, yellow, green, red, brown, and black pixel probabilities

Optimization: reviewed raster.

| Control | Default | Search status |
|---|---|---|
| Colour probability sharpness (`colour_temperature`) | 18.0 | Eligible when reviewed targets are available |

